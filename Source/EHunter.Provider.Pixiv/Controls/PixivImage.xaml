<UserControl
    x:Class="EHunter.Provider.Pixiv.Controls.PixivImage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:EHunter.Provider.Pixiv.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <UserControl.ContextFlyout>
        <MenuFlyout>
            <MenuFlyoutItem>
                <MenuFlyoutItem.Command>
                    <StandardUICommand Kind="Copy" ExecuteRequested="{x:Bind CopyRequested}" />
                </MenuFlyoutItem.Command>
            </MenuFlyoutItem>
            <MenuFlyoutItem Text="Refresh" Icon="Refresh" Click="{x:Bind RefreshImage}" />
        </MenuFlyout>
    </UserControl.ContextFlyout>
    <Image x:Name="image"
           HorizontalAlignment="Stretch"
           VerticalAlignment="Stretch" />
</UserControl>
