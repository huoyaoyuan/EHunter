<UserControl
    x:Class="EHunter.Provider.Pixiv.Controls.IllustSearchTargetPresenter"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <TextBlock x:Name="text">
        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup>
                <VisualState x:Name="PartialTag">
                    <VisualState.Setters>
                        <Setter Target="text.Text" x:Uid="IllustSearchTarget_PartialTag" />
                    </VisualState.Setters>
                </VisualState>
                <VisualState x:Name="ExactTag">
                    <VisualState.Setters>
                        <Setter Target="text.Text" x:Uid="IllustSearchTarget_ExactTag" />
                    </VisualState.Setters>
                </VisualState>
                <VisualState x:Name="TitleCaption">
                    <VisualState.Setters>
                        <Setter Target="text.Text" x:Uid="IllustSearchTarget_TitleCaption" />
                    </VisualState.Setters>
                </VisualState>
                <VisualState x:Name="_" />
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>
    </TextBlock>
</UserControl>
