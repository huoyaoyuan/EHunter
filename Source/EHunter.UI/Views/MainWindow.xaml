<Window
    x:Class="EHunter.UI.Views.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:EHunter.UI.Views"
    xmlns:vm ="using:EHunter.UI.ViewModels"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <TabView TabItemsSource="{x:Bind _vm.Tabs}"
             CanReorderTabs="True"
             AddTabButtonClick="{x:Bind _vm.AddNewTab}"
             TabCloseRequested="{x:Bind _vm.CloseTab}">
        <TabView.TabItemTemplate>
            <DataTemplate x:DataType="vm:TabRootVM"
                          x:DefaultBindMode="OneWay">
                <TabViewItem Header="{x:Bind Header}">
                    <local:TabRootView ViewModel="{x:Bind}" />
                </TabViewItem>
            </DataTemplate>
        </TabView.TabItemTemplate>
    </TabView>
</Window>
